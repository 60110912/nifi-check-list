org.apache.nifi.avro.AvroReader.json
schema = {
        "type": "object",
        "properties" : {
            "componentType":{"type": "string", "enum": ["CONTROLLER_SERVICE"]},
            "properties":{
                "type" :"object",
                "properties" : {
                    "schema-name":{"type": "string", "enum": ['${schema.name}'] },
                    "schema-text":{"type": "string", "enum": ['${avro.schema}'] }
                },
                "required": ["schema-name", "schema-text"]
            },
        },
        "required": ["componentType"],
}